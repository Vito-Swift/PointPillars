from setuptools import setup, find_packages

VERSION = '0.0.1'
DESCRIPTION = "PointPillars"

setup(
    name="PointPillars",
    version=VERSION,
    author="vitowu",
    description=DESCRIPTION,
    packages=find_packages(),
    install_requires=[],
)
